推荐方案：在现有 SGER 实现上收敛成 --use_sger_lite / Stage3-only 路径。

  - Stage1、Stage2：不运行 NormalHead，不创建/输出 SGER 字段，depth 保持原始 CasMVSNet 输出，并继续作为下一阶段采样中心。
  - Stage3：运行 NormalHead + SGERBlock，保存 depth_raw、depth_refined、depth_residual、geometry_gate 等；顶层 outputs["depth"] 默认使用 refined depth，但同时保留 raw 便于训练、日志和
    测试导出。

  - 不使用 refined depth 跨阶段反馈：因为只在 Stage3 refine，所以 Stage1->Stage2->Stage3 的搜索中心天然全是 raw depth。
  - 默认实验参数改成你给的 Lite 值：sger_max_residual_ratio=0.5，raw_depth_loss_weight=1.0，refined_depth_loss_weight=1.0，residual_loss_weight=0.005。
  - 增加 --freeze_backbone_epochs，前 6 个 epoch 只训练 Stage3 NormalHead + SGERBlock，之后解冻全模型。
  - 测试导出同时保存 depth_est_raw/、depth_est/，并增加残差校准置信度 confidence_residual_calibrated/；融合时用参数选择 confidence 或校准置信度，便于跑两套点云对比。
  - checkpoint 初始化支持直接从实验 8 checkpoint 加载：SGER Lite 新增参数缺失允许，原 CasMVSNet 权重严格对齐，避免静默吞掉主干不匹配。


python test.py \
    --dataset=general_eval \
    --batch_size=1 \
    --testpath=data/DTU/dtu_test_all \
    --testlist=lists/dtu/test.txt \
    --loadckpt=./checkpoints/cas_dtu/model_000012.ckpt \
    --outdir=./outputs \
    --interval_scale=1.06 \
    --filter_method=gipuma \
    --use_sger_lite \
    --fusion_depth_source refined \
    --fusion_confidence_source residual_calibrated
   目前：A
   执行python test.py   --dataset=general_eval   --batch_size=1   --testpath=data/DTU/dtu_test_all   --testlist=lists/dtu/test.txt   --loadckpt=./checkpoints/cas_dtu/model_000012.ckpt
  --outdir=./outputs   --interval_scale=1.06   --filter_method=gipuma --use_sger_lite时使用的时raw depth 还是 refined depth，原始置信度还是残差校准置信度？
• 这条命令使用的是：
  - 深度图保存/主输出：depth_est/ 保存的是 refined depth
  - 同时额外保存：depth_est_raw/ 保存的是 Stage3 raw depth
  - Gipuma 融合实际使用的深度：refined depth
  - Gipuma 融合实际使用的置信度：原始 photometric confidence

  原因是你用了：
  --filter_method=gipuma
  当前 gipuma_filter() 走的是 gipuma.py，它固定读取：
  depth_est/
  confidence/
  所以即使你开启了 --use_sger_lite，它会用：
  - depth_est/：refined depth
  - confidence/：原始置信度

消融B：refined depth + residual calibrated（精细后的置信度）
